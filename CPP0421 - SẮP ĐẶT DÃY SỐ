#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <algorithm>
#include <map>
using namespace std;
int main()
{
    int m;
    cin >> m;
    cin.ignore();
    while (m--)
    {
        long long n, x;
        map<long long, long long> mp;
        cin >> n;
        vector<int> a(n);
        for (long long i = 0; i < n; i++)
        {
            cin >> x;
            mp[x]++;
        }
        for (long long i = 0; i < n; i++)
        {
            if (mp[i] != 0)
            {
                a[i] = i;
            }
            else
            {
                a[i] = -1;
            }
        }
        for (int i = 0; i < n; i++)
        {
            cout << a[i] << " ";
        }
        cout << endl;
    }
}
