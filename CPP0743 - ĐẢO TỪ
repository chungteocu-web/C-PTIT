#include <iostream>
#include <sstream>
#include <algorithm>
#include <string>
#include <vector>
using namespace std;

int main()
{
    int m;
    cin >> m;
    cin.ignore(1);
    while (m--)
    {
        string s, tmp;
        getline(cin, s);
        vector<string> s1;
        stringstream ss(s);
        while (ss >> tmp)
        {
            s1.push_back(tmp);
        }
        for (int i = s1.size() - 1; i >= 0; i--)
        {
            cout << s1[i] << " ";
        }
        cout << endl;
    }
}
