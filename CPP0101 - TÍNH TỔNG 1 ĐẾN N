#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int a;
    cin >> a;
    while (a--)
    {
        long long b;
        cin >> b;
        long long sum = (b * (2 + (b - 1)) / 2);
        cout << sum << endl;
    }
}
