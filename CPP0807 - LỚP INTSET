#include <iostream>
#include <fstream>
#include <string>
#include <set>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    ifstream f1("DATA.in");
    int a = 1, b = 1;
    f1 >> a >> b;
    set<int> sc;
    vector<int> s1(a), s2(b);
    for (int i = 0; i < a; i++)
    {
        f1 >> s1[i];
    }
    for (int i = 0; i < b; i++)
    {
        f1 >> s2[i];
    }
    for (int i = 0; i < a; i++)
    {
        for (int j = 0; j < b; j++)
        {
            if (s1[i] == s2[j])
            {
                sc.insert(s1[i]);
                break;
            }
        }
    }
    for (auto it : sc)
    {
        cout << it << " ";
    }
}
