#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
#include <set>
using namespace std;
int main()
{
    int m;
    cin >> m;
    while (m--)
    {
        string b;
        long long a;
        cin >> a >> b;
        while (a != 0)
        {
            long long tmp = 0;
            for (int i = 0; i < b.size(); i++)
            {
                tmp = (tmp * 10 + (b[i] - '0')) % a;
            }
            b = to_string(a);
            a = tmp;
        }
        cout << b << endl;
    }
}
