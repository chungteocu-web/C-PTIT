#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <algorithm>
using namespace std;
int main()
{
    int m;
    cin >> m;
    cin.ignore();
    while (m--)
    {
        int b;
        cin >> b;
        cin.ignore();
        string a;
        getline(cin, a);
        int dem[10] = {0};
        for (int i = 0; i < a.size(); i++)
        {
            int tmp = a[i] - '0';
            if (tmp >= 0 && tmp <= 9)
            {
                dem[tmp]++;
            }
        }
        for (int i = 0; i < 10; i++)
        {
            if (dem[i] != 0)
            {
                cout << i << " ";
            }
        }
        cout << endl;
    }
}
