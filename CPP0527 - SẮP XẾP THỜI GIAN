#include <iostream>
#include <iomanip>
#include <algorithm>
#include <vector>
#include <string>
#include <sstream>
using namespace std;
struct Gio
{
    int gio, phut, giay;
};
bool sosanh(Gio a, Gio b)
{
    if (a.gio < b.gio)
        return 1;
    if (a.gio == b.gio)
    {
        if (a.phut < b.phut)
            return 1;
        if (a.phut == b.phut)
        {
            if (a.giay < b.giay)
                return 1;
        }
    }
    return 0;
}
void nhap(Gio &a)
{
    cin >> a.gio;
    cin >> a.phut;
    cin >> a.giay;
}
int main()
{
    int n;
    cin >> n;
    struct Gio ds[n];
    for (int i = 0; i < n; i++)
    {
        nhap(ds[i]);
    }
    sort(ds, ds + n, sosanh);
    for (int i = 0; i < n; i++)
    {
        cout << ds[i].gio << " " << ds[i].phut << " " << ds[i].giay << endl;
    }

    return 0;
}
