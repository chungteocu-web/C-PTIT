#include <iostream>
#include <string>
#include <sstream>
using namespace std;
int main()
{
    int count = 0, k;
    string s1, s2;
    getline(cin, s1);
    getline(cin, s2);
    s1.erase(s1.find(s2), s2.size());
    cout << s1 << endl;
}
