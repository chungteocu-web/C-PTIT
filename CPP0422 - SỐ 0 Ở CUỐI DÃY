#include <iostream>
#include <vector>
#include <algorithm>
#include <map>
using namespace std;
int main()
{
    int m;
    cin >> m;
    cin.ignore();
    while (m--)
    {
        long long n, x, b = 0;
        map<long long, long long> mp;
        cin >> n;
        vector<long long> a;
        for (int i = 0; i < n; i++)
        {
            cin >> x;
            if (x != 0)
            {
                ++b;
                cout << x << " ";
            }
        }
        for (int i = 0; i < n - b; i++)
        {
            cout << "0 ";
        }
        cout << endl;
    }
}
